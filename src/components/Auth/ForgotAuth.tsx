import { MessageType } from "./types"

export default function ForgotAuth({ setMsg }: { setMsg: (msg: MessageType) => void }) {
    return (
        <div>
            <h1>ForgotAuth</h1>
        </div>
    )
}